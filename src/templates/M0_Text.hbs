<div class="text-component">
    {{#if header}}
    <div class="component-header">
        <h2>{{{header}}}</h2>
    </div>
    {{/if}}{{#if body}}
    <div class="component-body">
        {{{body}}}
    </div>{{/if}}
</div>